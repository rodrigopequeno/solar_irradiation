//ignore_for_file: unused_import
import 'package:flutter/material.dart';
import 'package:flutter_svg/flutter_svg.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:solar_irradiation/app/app_module.dart';

import 'package:solar_irradiation/app/view/splash_screen/splash_screen_page.dart';

//TODO: Create tests for the splashscreen
main() {
  // initModule(AppModule());

  // group('widget visibility', () {
  //   testWidgets('display soon during the splash screen', (tester) async {
  //     await tester.pumpWidget(buildTestableWidget(SplashScreenPage()));
  //     final logoWidget = find.byType(SvgPicture);
  //     expect(logoWidget, findsOneWidget);
  //   });

  //   testWidgets('display circular progress during splash screen',
  //       (tester) async {
  //     await tester.pumpWidget(MaterialApp(home: SplashScreenPage()));
  //     final circularProgressIndicator = find.byType(CircularProgressIndicator);
  //     expect(circularProgressIndicator, findsNWidgets(2));
  //   });
  // });
}
